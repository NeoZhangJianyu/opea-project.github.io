name: Publish
permissions: {}

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
    steps:
    - uses: actions/checkout@v3
    - name: Build Online Document
      run: |
        #git config --local --get remote.origin.url
        pwd
        mkdir opea
        pwd
        cd opea
        pwd
        git clone https://github.com/NeoZhangJianyu/docs.git
        ls
        cd docs
        ls
        pwd
        bash scripts/build.sh

    - name: Push to github
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./opea/release_doc
        publish_branch: main
